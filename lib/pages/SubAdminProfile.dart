import 'package:flutter/material.dart';

class SubAdminProfile extends StatelessWidget {
  const SubAdminProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("SubAdminProfile"),
      ),
    );
  }
}
