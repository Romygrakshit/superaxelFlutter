import 'package:flutter/material.dart';

class CreateInventory extends StatelessWidget {
  const CreateInventory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Create Inventory Page")),
    );
  }
}
